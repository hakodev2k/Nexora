# identity / platform — physical data dictionary

> Current specification · reconciled 2026-09-08 · Docs-only. [Previous version](../history/20260908/snapshot/docs/design-database/02-core-identity-platform.md) is historical evidence, not implementation input.

Review 2026-09-07 · Baseline `b85f0f314da8ca7dcee8dad156e7b538ba52c287` · Documentation only; no schema, migrations or application code executed.

Technical design for SQL Server. Business rules retain their source status; rows marked Q are conditional proposals, not approval. Every physical column, including inherited technical columns, is expanded below. [Conventions](01-conventions-and-integrity.md) · [Relations](11-relations-and-transactions.md) · [Payload contracts](14-payload-contracts.md).

| Table | Purpose | Feature | Gate |
| --- | --- | --- | --- |
| [identity.User](#identity-user) | Authentication principal; not a business-data owner | FX-01, FX-02 | Technical decision |
| [identity.Session](#identity-session) | Revocable server-authoritative browser session | FX-01 | Technical decision |
| [identity.OneTimeToken](#identity-onetimetoken) | Verification/password-reset proof | FX-01 | Technical decision |
| [identity.Role](#identity-role) | Fixed system role catalog | FX-02 | Technical decision |
| [identity.UserRole](#identity-userrole) | System role membership | FX-02 | Technical decision |
| [platform.PersonalSpace](#platform-personalspace) | Exactly one personal ownership boundary per verified User | FX-01, FX-03 | Technical decision |
| [platform.SecurityInvariant](#platform-securityinvariant) | Serialization point for security invariants | FX-02 | Technical decision |
| [platform.Module](#platform-module) | Trusted developer-installed module identity | FX-03 | Technical decision |
| [platform.ModuleRelease](#platform-modulerelease) | Immutable trusted deployment metadata | FX-03 | Technical decision |
| [platform.ModuleDependency](#platform-moduledependency) | Declared hard/optional dependency graph | FX-03 | Technical decision |
| [platform.ModuleMigration](#platform-modulemigration) | Per-module migration journal design | FX-03, FX-10 | Technical decision |
| [platform.UserModuleGrant](#platform-usermodulegrant) | Per-user module enablement snapshot | FX-02, FX-03 | Technical decision |
| [platform.Permission](#platform-permission) | Developer-declared action catalog | FX-02, FX-03 | Technical decision |
| [platform.AdminPermission](#platform-adminpermission) | Explicit Admin action allow/deny | FX-02 | Technical decision |
| [platform.ResourceType](#platform-resourcetype) | Typed resource capability registration | FX-03, FX-04, FX-25 | Technical decision |
| [platform.Resource](#platform-resource) | Identity/lifecycle directory, not generic content store | FX-03, FX-04, FX-08, FX-25 | Technical decision |
| [platform.ResourceLink](#platform-resourcelink) | Typed cross-module relationship guarded by providers | FX-03, FX-24 | Technical decision |
| [platform.Preference](#platform-preference) | Schema-validated non-secret preferences | FX-09 | Technical decision |
| [identity.AccountMessageIntent](#identity-accountmessageintent) | Pre-activation verification/recovery transactional communication | FX-01 | Technical decision |
| [identity.MfaCredential](#identity-mfacredential) | Google Authenticator TOTP confirmed; enrollment design subject to Q-02-R release gate | FX-01 | Proposed: Q-02 |
| [identity.RecoveryCode](#identity-recoverycode) | One-use account MFA recovery proof | FX-01 | Proposed: Q-02 |
| [platform.SystemConnection](#platform-systemconnection) | Operator provider configuration separate from personal integrations | FX-35 | Proposed: Q-07/Q-08 provider selection |

<a id="identity-user"></a>
## identity.User

Authentication principal; not a business-data owner. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| Email | nvarchar(320) | No | Original verified-contact candidate | No implicit default unless stated |
| NormalizedEmail | nvarchar(320) | No | Invariant normalized lookup value | No implicit default unless stated |
| PasswordHash | nvarchar(1024) | No | Framework-versioned one-way hash, never reversible password | No implicit default unless stated |
| SecurityStamp | nvarchar(64) | No | Invalidate authentication proofs after credential/security changes | No implicit default unless stated |
| State | varchar(64) | No | PendingVerification, Active, Disabled, Deleted; State=Deleted iff IsDeleted=true | CHECK allowed codes documented in meaning |
| VerifiedAt | datetime2(7) | Yes | Null until successful verification | No implicit default unless stated |
| FailedAccessCount | int | No | Nonnegative failed login counter | No implicit default unless stated |
| LockoutUntil | datetime2(7) | Yes | Temporary lock expiry | No implicit default unless stated |
| DisplayName | nvarchar(100) | No | Profile name1..100, never role | No implicit default unless stated |
| TimeZoneId | nvarchar(100) | No | Validated IANA identifier detected from browser | No implicit default unless stated |
| Locale | nvarchar(35) | No | UI locale vi/en, independent of timezone/currency | DEFAULT 'vi'; CHECK vi/en |
| EmailConfirmed | bit | No | Must agree with VerifiedAt | No implicit default unless stated |
| AvatarFileId | uniqueidentifier | Yes | Optional Clean own avatar, target FileObject.OwnerId must equal this User PersonalSpace via profile command guard | FK AvatarFileId → Id; [files.FileObject](04-files-jobs-notifications.md#files-fileobject); NO ACTION |
| IsDeleted | bit | No | Authoritative soft-delete flag; State agreement | DEFAULT 0 |
| DeletedAt | datetime2(7) | Yes | Required iff IsDeleted=true; UTC | Server timestamp |
| DeletedByUserId | uniqueidentifier | Yes | Actual deletion actor | FK identity.User.Id; NO ACTION |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ NormalizedEmail; IX State,Id

**Integrity / transaction:** Create only PendingVerification. Verify token consumption, activation, PersonalSpace and registration-grant snapshot in one transaction. No role/profile mass assignment. Account purge prohibited by PO; identifier reuse/account restore remain separate proposal.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-user). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-session"></a>
## identity.Session

Revocable server-authoritative browser session. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Authenticated subject | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| HandleHash | binary(32) | No | Digest of random cookie handle, not raw session | No implicit default unless stated |
| IssuedAt | datetime2(7) | No | Authentication instant | No implicit default unless stated |
| ExpiresAt | datetime2(7) | No | Absolute expiry | No implicit default unless stated |
| IdleExpiresAt | datetime2(7) | No | Server-authoritative idle expiry | No implicit default unless stated |
| RevokedAt | datetime2(7) | Yes | Revocation is immediate | No implicit default unless stated |
| RecentAuthenticatedAt | datetime2(7) | Yes | Last successful step-up | No implicit default unless stated |
| DeviceLabel | nvarchar(200) | Yes | User-visible sanitized device description | No implicit default unless stated |
| LastSeenAt | datetime2(7) | No | Throttled activity marker | No implicit default unless stated |
| SecurityStamp | nvarchar(64) | No | Stamp at issue | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ HandleHash; IX UserId,RevokedAt,ExpiresAt

**Integrity / transaction:** Cookie protection keys outside business DB; no token in localStorage. Recheck SQL subject/account state and revocation on every protected request; policy durations ADR/Q-02.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-session). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-onetimetoken"></a>
## identity.OneTimeToken

Verification/password-reset proof. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Recipient | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| Purpose | varchar(64) | No | VerifyEmail, ChangeEmail or ResetPassword | CHECK allowed codes documented in meaning |
| TokenHash | binary(32) | No | Digest only | No implicit default unless stated |
| ExpiresAt | datetime2(7) | No | Single-use validity boundary | No implicit default unless stated |
| ConsumedAt | datetime2(7) | Yes | Conditional consume marker | No implicit default unless stated |
| EmailSnapshot | nvarchar(320) | No | Target address bound to proof | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ TokenHash; IX UserId,Purpose,ExpiresAt

**Integrity / transaction:** Only consume if purpose, target email, expiry and unconsumed state match under transaction. Verify TTL24h/resend60s; Reset30min delegated defaults remain security-gated. Resend invalidates earlier same-purpose proofs. ChangeEmail verifies new EmailSnapshot before replacing old email atomically under normalized uniqueness, preserves roles/grants and existing PersonalSpace. Generic recovery responses do not enumerate accounts.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-onetimetoken). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-role"></a>
## identity.Role

Fixed system role catalog. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| Code | varchar(64) | No | SuperAdmin, Admin, User | CHECK allowed codes documented in meaning |
| Description | nvarchar(500) | No | Administrative explanation | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ Code

**Integrity / transaction:** Roles do not imply personal-data access. No Workspace roles.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-role). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-userrole"></a>
## identity.UserRole

System role membership. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Principal | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RoleId | uniqueidentifier | No | System role | FK RoleId → Id; [identity.Role](02-core-identity-platform.md#identity-role); NO ACTION |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ UserId,RoleId

**Integrity / transaction:** Serialize changes against platform.SecurityInvariant singleton; never remove/disable last active SuperAdmin.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-userrole). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-personalspace"></a>
## platform.PersonalSpace

Exactly one personal ownership boundary per verified User. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Unique owning account | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| State | varchar(64) | No | Active, Suspended; Deleted User suspends retained space | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ UserId

**Integrity / transaction:** No Workspace, membership, organization or owner-type discriminator. A GUID is not authorization.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-personalspace). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-securityinvariant"></a>
## platform.SecurityInvariant

Serialization point for security invariants. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| Code | varchar(64) | No | LastActiveSuperAdmin singleton | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ Code

**Integrity / transaction:** Transaction obtains update lock before counting active SuperAdmins and changing roles/account states. No cached count authority.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-securityinvariant). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-module"></a>
## platform.Module

Trusted developer-installed module identity. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| Code | nvarchar(100) | No | Stable namespaced module key | No implicit default unless stated |
| DisplayName | nvarchar(200) | No | Navigation label | No implicit default unless stated |
| InstalledVersion | nvarchar(50) | No | Deployed semver | No implicit default unless stated |
| ContractVersion | nvarchar(50) | No | Platform contract version | No implicit default unless stated |
| State | varchar(64) | No | Installed, Ready, Disabled, DependencyFailed, UpgradePending, MigrationFailed | CHECK allowed codes documented in meaning |
| SystemEnabled | bit | No | System availability gate | No implicit default unless stated |
| SharingEnabled | bit | No | SuperAdmin sharing capability policy | No implicit default unless stated |
| RegistrationEnabled | bit | No | Default for newly verified accounts | No implicit default unless stated |
| PolicyRevision | bigint | No | Monotonic enablement policy epoch | No implicit default unless stated |
| SharingEpoch | bigint | No | Positive; increment when sharing disabled; never reset on re-enable | DEFAULT 1 |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ Code

**Integrity / transaction:** At verification grant defaults only for installed, compatible, health-ready and current active-scope modules; Paused/Blocked never enabled. New future modules are not silently R1 scope. Disabled retains data; executable upload forbidden.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-module). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-modulerelease"></a>
## platform.ModuleRelease

Immutable trusted deployment metadata. Profile **A**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| ModuleId | uniqueidentifier | No | Installed module | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| Version | nvarchar(50) | No | Semver | No implicit default unless stated |
| ManifestJson | nvarchar(max) | No | Versioned developer-owned manifest, no user code | ISJSON + versioned allowlist; see payload contracts |
| ArtifactDigest | binary(32) | No | Build content digest | No implicit default unless stated |
| Compatibility | nvarchar(200) | No | Platform version interval | No implicit default unless stated |
| RecordedAt | datetime2(7) | No | Deployment journal time | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ ModuleId,Version

**Integrity / transaction:** Manifest JSON is configuration contract, not dynamic business schema.

**Lifecycle / classification:** Append-only; no in-place update/restore of an old row. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-modulerelease). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-moduledependency"></a>
## platform.ModuleDependency

Declared hard/optional dependency graph. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | No | Consumer | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| DependencyModuleId | uniqueidentifier | No | Provider | FK DependencyModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| VersionRange | nvarchar(100) | No | Supported contract interval | No implicit default unless stated |
| Kind | varchar(64) | No | Hard or Optional | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ ModuleId,DependencyModuleId

**Integrity / transaction:** No self-edge/cycles; hard dependency must be Ready. Optional contribution fails locally.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-moduledependency). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-modulemigration"></a>
## platform.ModuleMigration

Per-module migration journal design. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | No | Owning module | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| MigrationKey | nvarchar(200) | No | Ordered immutable migration identifier | No implicit default unless stated |
| Checksum | binary(32) | No | Approved migration checksum | No implicit default unless stated |
| State | varchar(64) | No | Pending, Running, Applied, Failed | CHECK allowed codes documented in meaning |
| StartedAt | datetime2(7) | Yes | Attempt start | No implicit default unless stated |
| CompletedAt | datetime2(7) | Yes | Success time | No implicit default unless stated |
| ErrorCode | nvarchar(100) | Yes | Redacted failure code | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ ModuleId,MigrationKey

**Integrity / transaction:** No migrations run in this docs phase. Exclusive deployment lease; checksum drift stops deployment.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-modulemigration). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-usermodulegrant"></a>
## platform.UserModuleGrant

Per-user module enablement snapshot. Profile **O**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| OwnerId | uniqueidentifier | No | Required isolation boundary resolved server-side, immutable | FK OwnerId → Id; [platform.PersonalSpace](02-core-identity-platform.md#platform-personalspace); NO ACTION |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | No | Module | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| Enabled | bit | No | Personal module gate | No implicit default unless stated |
| RegistrationPolicyRevision | bigint | Yes | Policy used at account provisioning | No implicit default unless stated |
| GrantRevision | bigint | No | Revocation/concurrency epoch | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ(OwnerId,Id); IX(OwnerId,CreatedAt,Id). UQ OwnerId,ModuleId

**Integrity / transaction:** OwnerId resolves from target account by privileged policy command, never arbitrary user payload.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Private owner; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-usermodulegrant). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-permission"></a>
## platform.Permission

Developer-declared action catalog. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | No | Owning module | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| Code | nvarchar(150) | No | Stable module.action key | No implicit default unless stated |
| Description | nvarchar(500) | No | Action meaning | No implicit default unless stated |
| Risk | varchar(64) | No | Normal, Sensitive, Administrative | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ Code

**Integrity / transaction:** Unknown action deny. Metadata does not grant permission by itself.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-permission). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-adminpermission"></a>
## platform.AdminPermission

Explicit Admin action allow/deny. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Admin principal | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| PermissionId | uniqueidentifier | No | Action | FK PermissionId → Id; [platform.Permission](02-core-identity-platform.md#platform-permission); NO ACTION |
| Effect | varchar(64) | No | Allow or Deny | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ UserId,PermissionId

**Integrity / transaction:** Explicit deny wins; no access_all personal-data permission. SuperAdmin policy actions still do not supply owner context.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-adminpermission). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-resourcetype"></a>
## platform.ResourceType

Typed resource capability registration. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | No | Provider | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| Code | nvarchar(100) | No | Stable resource type | No implicit default unless stated |
| ContractVersion | nvarchar(50) | No | Projection/command contract | No implicit default unless stated |
| CapabilitiesJson | nvarchar(max) | No | Validated flags and projection names, not executable handler code | ISJSON + versioned allowlist; see payload contracts |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ ModuleId,Code

**Integrity / transaction:** Capabilities include share/search/trash/history/support-safe eligibility; default deny.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-resourcetype). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-resource"></a>
## platform.Resource

Identity/lifecycle directory, not generic content store. Profile **O**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| OwnerId | uniqueidentifier | No | Required isolation boundary resolved server-side, immutable | FK OwnerId → Id; [platform.PersonalSpace](02-core-identity-platform.md#platform-personalspace); NO ACTION |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ResourceTypeId | uniqueidentifier | No | Typed provider | FK ResourceTypeId → Id; [platform.ResourceType](02-core-identity-platform.md#platform-resourcetype); NO ACTION |
| Availability | varchar(64) | No | Active, Archived, Trash, Purged | CHECK allowed codes documented in meaning |
| Revision | bigint | No | Source command revision | No implicit default unless stated |
| PurgedAt | datetime2(7) | Yes | Tombstone instant | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ(OwnerId,Id); IX(OwnerId,CreatedAt,Id). IX OwnerId,ResourceTypeId,Availability,Id

**Integrity / transaction:** Registered aggregate Id equals this Id and owner; domain payload remains typed module tables. No title/body/secret here. Registry lifecycle changes atomically with source. Tombstones keep minimal identity only; retention/account deletion Q-01/Q-08.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Private owner; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-resource). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-resourcelink"></a>
## platform.ResourceLink

Typed cross-module relationship guarded by providers. Profile **O**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| OwnerId | uniqueidentifier | No | Required isolation boundary resolved server-side, immutable | FK OwnerId → Id; [platform.PersonalSpace](02-core-identity-platform.md#platform-personalspace); NO ACTION |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| SourceResourceId | uniqueidentifier | No | Source identity | FK (OwnerId, SourceResourceId) → (OwnerId, Id); [platform.Resource](02-core-identity-platform.md#platform-resource); NO ACTION |
| TargetResourceId | uniqueidentifier | No | Target identity | FK (OwnerId, TargetResourceId) → (OwnerId, Id); [platform.Resource](02-core-identity-platform.md#platform-resource); NO ACTION |
| RelationType | nvarchar(100) | No | Developer-registered relation | No implicit default unless stated |
| TargetVersion | bigint | Yes | Optional exact immutable version | No implicit default unless stated |
| State | varchar(64) | No | Active, Unavailable, Detached | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ(OwnerId,Id); IX(OwnerId,CreatedAt,Id). UQ OwnerId,SourceResourceId,TargetResourceId,RelationType,TargetVersion

**Integrity / transaction:** Both references same owner. Registry FK guarantees identity, provider contract validates type/lifecycle/version. Purge marks unavailable; no cascade across modules.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Private owner; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-resourcelink). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-preference"></a>
## platform.Preference

Schema-validated non-secret preferences. Profile **O**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| OwnerId | uniqueidentifier | No | Required isolation boundary resolved server-side, immutable | FK OwnerId → Id; [platform.PersonalSpace](02-core-identity-platform.md#platform-personalspace); NO ACTION |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ModuleId | uniqueidentifier | Yes | Null for global preferences | FK ModuleId → Id; [platform.Module](02-core-identity-platform.md#platform-module); NO ACTION |
| PreferenceKey | nvarchar(100) | No | Registered key | No implicit default unless stated |
| SchemaVersion | int | No | Payload contract | No implicit default unless stated |
| ValueJson | nvarchar(max) | No | Theme, view, navigation or locale settings only | ISJSON + versioned allowlist; see payload contracts |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ(OwnerId,Id); IX(OwnerId,CreatedAt,Id). UQ OwnerId,ModuleId,PreferenceKey

**Integrity / transaction:** No arbitrary settings bag for business entities, credentials or notification channel opt-out. Unique null-module scope enforced by filtered indexes.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Private owner; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-preference). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-accountmessageintent"></a>
## identity.AccountMessageIntent

Pre-activation verification/recovery transactional communication. Profile **G**. Status: **Technical decision**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Pending or verified recipient | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| TokenId | uniqueidentifier | Yes | Verification/reset proof reference | FK TokenId → Id; [identity.OneTimeToken](02-core-identity-platform.md#identity-onetimetoken); NO ACTION |
| Kind | varchar(64) | No | Verification, Recovery | CHECK allowed codes documented in meaning |
| IntentKey | nvarchar(200) | No | Stable command-derived key | No implicit default unless stated |
| State | varchar(64) | No | Pending, Sent, Failed | CHECK allowed codes documented in meaning |
| DueAt | datetime2(7) | No | Dispatch instant | No implicit default unless stated |
| ErrorCode | nvarchar(100) | Yes | Redacted category | No implicit default unless stated |
| DeliveryEnvelope | varbinary(max) | Yes | Short-lived encrypted verification/reset token+destination for dispatch retry, hash alone cannot reconstruct link | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ UserId,IntentKey; IX State,DueAt

**Integrity / transaction:** Pre-activation transactional Email plus a separate safe three-channel security-event intent if applicable. DeliveryEnvelope is authenticated encryption under external service key, bound User/token/purpose/expiry; worker decrypts only to send. Remove encrypted payload after accepted send or token expiry/consume; never reconstruct a token from its hash. No fake successful Browser/In-app delivery before activation.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-accountmessageintent). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-mfacredential"></a>
## identity.MfaCredential

Google Authenticator TOTP confirmed. Profile **G**. Status: **Technical schema and M01 read guard; enrollment/recovery release blocked Q-02-R**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Principal | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| Method | varchar(64) | No | Totp; confirmed method, CHECK Method = 'Totp' | CHECK allowed codes documented in meaning |
| SecretEnvelope | varbinary(max) | No | Encrypted authenticator seed under reviewed key service | No implicit default unless stated |
| ConfirmedAt | datetime2(7) | Yes | Null pending enrollment proof | No implicit default unless stated |
| DisabledAt | datetime2(7) | Yes | Explicit removal after step-up | No implicit default unless stated |
| LastAcceptedStep | bigint | Yes | Replay protection counter | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. IX UserId,Method,DisabledAt

**Integrity / transaction:** Q-02 decides enrollment/recovery requirements. Setup seed revealed only once in recent-auth protected enrollment view, never Audit/Search/Support.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-mfacredential). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="identity-recoverycode"></a>
## identity.RecoveryCode

One-use account MFA recovery proof. Profile **G**. Status: **Proposed: Q-02**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| UserId | uniqueidentifier | No | Principal | FK UserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| BatchId | uniqueidentifier | No | Regeneration cohort | No implicit default unless stated |
| CodeHash | binary(32) | No | Salted/peppered security-reviewed digest, not plaintext | No implicit default unless stated |
| UsedAt | datetime2(7) | Yes | Conditional consume | No implicit default unless stated |
| RevokedAt | datetime2(7) | Yes | Regeneration invalidates previous codes | No implicit default unless stated |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ CodeHash; IX UserId,BatchId

**Integrity / transaction:** Q-02. Codes generated once shown owner; concurrent use succeeds once. Key/pepper outside SQL.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#identity-recoverycode). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

<a id="platform-systemconnection"></a>
## platform.SystemConnection

Operator provider configuration separate from personal integrations. Profile **G**. Status: **Proposed: Q-07/Q-08 provider selection**.

| Field | SQL Server type | Nullable | Meaning / validation | Key / default / reference |
| --- | --- | --- | --- | --- |
| Id | uniqueidentifier | No | Opaque stable PK generated by trusted command | No implicit default unless stated |
| CreatedAt | datetime2(7) | No | Server insert instant, DEFAULT SYSUTCDATETIME() | No implicit default unless stated |
| CreatedByUserId | uniqueidentifier | Yes | Actual actor for attribution, not owner; null only system/anonymized identity | FK CreatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| UpdatedAt | datetime2(7) | No | Server write instant, initially CreatedAt | No implicit default unless stated |
| UpdatedByUserId | uniqueidentifier | Yes | Actual most recent actor; null only system/anonymized identity | FK UpdatedByUserId → Id; [identity.User](02-core-identity-platform.md#identity-user); NO ACTION |
| RowVersion | rowversion | No | SQL-generated 8-byte optimistic concurrency token; not content history or clock | DB generated; exclude from inserts/updates |
| ProviderKey | nvarchar(150) | No | Registered system Email/Push/storage/ingestion adapter | No implicit default unless stated |
| Title | nvarchar(200) | No | Operator label | No implicit default unless stated |
| EndpointEnvelope | varbinary(max) | Yes | Protected provider endpoint if sensitive | No implicit default unless stated |
| SecretReference | nvarchar(300) | No | Configured external secret-service handle, not owner Vault plaintext | No implicit default unless stated |
| ConfigSchemaVersion | int | No | Provider DTO version | No implicit default unless stated |
| SettingsJson | nvarchar(max) | No | Allowlisted nonsecret configuration | ISJSON + versioned allowlist; see payload contracts |
| State | varchar(64) | No | Configured, Valid, Invalid, Disabled | CHECK allowed codes documented in meaning |

**Keys/index candidates:** PK(Id) nonclustered; internal clustering strategy in conventions. UQ ProviderKey,Title

**Integrity / transaction:** Only operational permission, cannot point to arbitrary owner Vault without explicit authorized contract. Provider/egress/cost decisions Q-07/Q-08.

**Lifecycle / classification:** All writes check RowVersion; lifecycle guard also applies to import, automation, bulk and restore. Payload classification defaults Restricted system; credential/encrypted/hash columns are never list/search/log data. [Per-field classification](15-field-classification.md#platform-systemconnection). No ON DELETE CASCADE; approved purge service orders dependencies, rejects live references, preserves minimal audit. User-owned Trash retention is not inferred from job-log retention.

## Scoped relationship diagrams

Each edge describes a declared FK, not authorization or cascade deletion. Composite owner keys and one-to-one/unique restrictions are normative in the dictionary. Diagrams split into small neighborhoods; the exhaustive edge index is in [relations](11-relations-and-transactions.md).

~~~mermaid
erDiagram
    direction TB
    identity_User ||--o{ identity_Session : "UserId"
    identity_User ||--o{ identity_OneTimeToken : "UserId"
    identity_User ||--o{ identity_UserRole : "UserId"
~~~

~~~mermaid
erDiagram
    direction TB
    identity_Role ||--o{ identity_UserRole : "RoleId"
    identity_User ||--o{ platform_PersonalSpace : "UserId"
    platform_Module ||--o{ platform_ModuleRelease : "ModuleId"
~~~

~~~mermaid
erDiagram
    direction TB
    platform_Module ||--o{ platform_ModuleDependency : "ModuleId"
    platform_Module ||--o{ platform_ModuleDependency : "DependencyModuleId"
    platform_Module ||--o{ platform_ModuleMigration : "ModuleId"
~~~

~~~mermaid
erDiagram
    direction TB
    platform_Module ||--o{ platform_UserModuleGrant : "ModuleId"
    platform_Module ||--o{ platform_Permission : "ModuleId"
    identity_User ||--o{ platform_AdminPermission : "UserId"
~~~

~~~mermaid
erDiagram
    direction TB
    platform_Permission ||--o{ platform_AdminPermission : "PermissionId"
    platform_Module ||--o{ platform_ResourceType : "ModuleId"
    platform_ResourceType ||--o{ platform_Resource : "ResourceTypeId"
~~~

~~~mermaid
erDiagram
    direction TB
    platform_Resource ||--o{ platform_ResourceLink : "SourceResourceId"
    platform_Resource ||--o{ platform_ResourceLink : "TargetResourceId"
    platform_Module o|--o{ platform_Preference : "ModuleId"
~~~

~~~mermaid
erDiagram
    direction TB
    identity_User ||--o{ identity_AccountMessageIntent : "UserId"
    identity_OneTimeToken o|--o{ identity_AccountMessageIntent : "TokenId"
    identity_User ||--o{ identity_MfaCredential : "UserId"
~~~

~~~mermaid
erDiagram
    direction TB
    identity_User ||--o{ identity_RecoveryCode : "UserId"
~~~

## MFA clarification — 2026-09-08

DEC-20260908-Q02-TOTP confirms Google Authenticator OTP as the optional MFA method. identity.MfaCredential stores an encrypted TOTP seed, never Google OAuth access/refresh tokens. Method = Totp; enrollment becomes enabled only after proof. Existing key/field design remains a technical specification; Q-02-R still blocks release pending the lost-device recovery policy. identity.RecoveryCode remains Proposed; confirming TOTP does not approve recovery codes, email-only MFA removal, or Admin reset of MFA. No schema or migration executed.
